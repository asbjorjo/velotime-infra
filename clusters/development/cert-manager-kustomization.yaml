apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  interval: 1h
  path: ./clusters/development/cert-manager
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # cilium must be reconciled with gatewayAPI.enabled first so the GatewayClass and CRDs are ready.
  dependsOn:
    - name: cilium
  timeout: 5m
